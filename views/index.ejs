<!DOCTYPE html>
<html lang="da">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AMAbot</title>
  <link rel="stylesheet" href="/css.css">
</head>
<body>

  <main class="app">
    <div class="app-header">
      <h1>Spørg mig om alt</h1>
      <form method="POST" action="/clear-messages">
        <button type="submit" class="reset-btn">Nulstil</button>
      </form>
    </div>

    <section id="chat">
      <% for (const message of messages) { %>
        <article class="msg <%= message.type %>">
          <span class="sender"><%= message.type === "question" ? "Dig" : "Bot" %>:</span>
          <%= message.text %>
          <% if (message.suggestions && message.suggestions.length) { %>
            <div class="suggestions">Spørg om
              <% message.suggestions.forEach(function (suggestion, index) { %><span class="keyword"><%= suggestion %></span><%= index < message.suggestions.length - 1 ? ", " : "" %><% }) %>
            </div>
          <% } %>
        </article>
      <% } %>
    </section>

    <% if (error) { %>
      <p role="alert" class="error"><%= error %></p>
    <% } %>

    <form method="POST" action="/ask" class="input-row">
      <label for="question" class="sr-only">Stil dit spørgsmål her:</label>
      <input id="question" name="question" type="text" placeholder="Spørg mig om noget..." autocomplete="off" />
      <button type="submit">Send</button>
    </form>
  </main>

</body>
</html>
